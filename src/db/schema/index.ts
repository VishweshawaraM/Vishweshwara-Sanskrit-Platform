/**
 * Drizzle schema barrel.
 *
 * DELIBERATELY EMPTY IN SPRINT 1 — no business tables until the foundation is
 * approved. The full design is specified in docs/07-database-design.md and will
 * land here as one file per domain:
 *
 *   identity.ts      person, auth_credential, session_token, guardian_link
 *   curriculum.ts    path, stage, syllabus_item
 *   enrollment.ts    enrollment, item_progress, stage_completion
 *   sessions.ts      study_group, session_series, session, session_participant
 *   abhyasa.ts       abhyasa_log, assignment, submission, review
 *   avalokanam.ts    avalokanam and its components, responses, results
 *   certificates.ts  certificate
 *   admissions.ts    application
 *   ops.ts           announcement, notification, audit_log, stored_file
 */

export {};
